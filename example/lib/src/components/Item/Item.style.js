import { ScreenWidth } from "@freakycoder/react-native-helpers";

export default {
  container: {
    width: ScreenWidth,
    alignSelf: "center",
    flexDirection: "row",
    justifyContent: "center"
  },
  insideListContainer: {
    marginTop: -24,
    flexDirection: "column"
  }
};
